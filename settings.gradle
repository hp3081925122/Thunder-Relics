pluginManagement {
    repositories {
        // 使用 NeoForge ModDevGradle 插件所需的官方仓库。
        gradlePluginPortal()
        maven { url = 'https://maven.neoforged.net/releases' }
    }
}

rootProject.name = 'thunderrelics'
